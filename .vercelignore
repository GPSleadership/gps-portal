# ── Non-deployed API helpers ────────────────────────────────
api/test-survey.js
api/run-reminders-now.js

# ── Internal docs (architecture, prompts, audits — NEVER serve) ──
# These describe how the system works and where the holes are.
# Treat them as credentials, not content.
*.md
*.MD

# ── Database migrations & data scripts (not served) ─────────
*.sql
*.SQL
data-change-*.sql

# ── Skills, build scripts, env templates ────────────────────
*.skill
scripts/
proposal_build_template.js
.env.example
check.sh
deploy.sh
smoke-test.sh
install-hooks.sh
deploy-allowed-pages.txt

# ── Backup / demo / dev pages with NO route rewrite (pure exposure) ──
client-DEMO.html
client-PRE-WIZARD-BACKUP-2026-06-01.html
onboarding-sandbox.html
workshop-sandbox.html
diagnostic-coach.html
gps-executive-console.html
gps-executive-console-deploy.html
gps-ea-console.html
control.html
_SANDBOX_new-features.html
ops-console-deploy/

# ── Legacy ROOT-LEVEL copies of api functions (2026-07-03 audit) ──
# Old duplicates of api/notify.js and api/get-client.js at the repo root were
# being served publicly as static files (exposing server source). Leading slash
# anchors to root so api/notify.js and api/get-client.js still deploy normally.
/notify.js
/get-client.js

# ── Stray extensionless zip blobs (P0-3, 2026-07-01 audit) — leak full
# source/schema/prompts; the *.md/*.sql rules don't reach inside a zip.
# Listed here for immediate deploy-safety; also git rm'd from the repo.
zi5BMnX1
zi9SwTvy
ziRezBZW
ziS1Y6yw
ziS3AIaJ
ziepZ4pD
ziw7HK1Q

# NOTE: coach-emergency.html, diagnostic-sandbox.html, and workshop-poc.html
# have intentional route rewrites in vercel.json and are LEFT deployed.
# Decide separately whether /coach-emergency and /diagnostic-sandbox should
# stay public — workshop-poc is a live client feature and must stay.
